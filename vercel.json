{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/bible-quiz/:path*", "destination": "/bible-quiz/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
